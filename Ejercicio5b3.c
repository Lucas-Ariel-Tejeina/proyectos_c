/*
El programa del Ejercicio5b1, plantea que dados 2 estados iniciales para las variables 
x e y, si x >= y entramos en un ciclo donde vamos reduciendo el estado de x hasta que 
x < y y si los estados inciales ya cumple que x < y no entramos en el ciclo y los estados
permanecen como estaban, o sea este programa busca que x sea menos extricto que y.

En el programa Ejercicio5b2, se busca saber si dado un estado incial a la varaible ese
valor es primo, ya que que x debe ser mayor que 2 para entrar en el ciclo, y una vez
que entramos se busca saber si existe algun numero entre [2,x) que divida a x, en el caso 
de que no exista tal numero el resultado que devuelve es true y caso contrario false.
*/